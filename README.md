Prioritizes suspects based on configured features. Can be trained on csv files and then used for prediction on csv files.
- Produces a report with most likely suspects highlighted in red.
- Can produce a blank template for input data to be inserted into.
- Currently trained on dummy data with ten features.


